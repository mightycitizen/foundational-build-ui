
  <fieldset class="options my-4 collapsible"  x-data="{ expanded: true }">
    <legend @click="expanded = ! expanded">
      {{ label }} {% if required | default %}<span class="form-required">*</span>{% endif %}
      <span class="icon-caret-down" :class="{ 'rotate-180': expanded }"></span>
    </legend>
    <div class="collapsible-group" x-show="expanded" x-collapse>
      <ul class="checkbox-group list-none">
        {% for option in options %}
          <li>{% include '@components/forms/checkbox/checkbox.twig' with {
           field_id: id,
           value: option.value,
           name: name,
           label: option.label
          } only %}</li>
        {% endfor %}
      </ul>
    </div>
    {# {% if required %}
      <span class="form-error">{{ label }} is required</span>
    {% endif %} #}
  </fieldset>

<div class="grid grid-cols-12  gap-6 align-{{ alignment | default('center') }}" data-equalizer data-equalize-on="medium">
  {% for card in cards %}
    {% set card = card | merge({lazyload_class: 'data-splide-lazy', equalize: true}) %}
    <div class="col-span-12 md:col-span-6">
      {% include "@components/teasers/card/card-horizontal.twig" with card only %}
    </div>
  {% endfor %}
</div>
